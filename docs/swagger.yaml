basePath: /v1
definitions:
  area.AreaEnvelope:
    properties:
      data:
        $ref: '#/definitions/area.AreaView'
      error: {}
    type: object
  area.AreaListEnvelope:
    properties:
      data:
        $ref: '#/definitions/area.ListAreasResponse'
      error: {}
    type: object
  area.AreaView:
    properties:
      color:
        type: string
      createdAt:
        type: string
      displayOrder:
        type: integer
      icon:
        type: string
      id:
        type: string
      name:
        type: string
      updatedAt:
        type: string
    type: object
  area.AreaWithProjectsEnvelope:
    properties:
      data:
        items:
          $ref: '#/definitions/area.AreaWithProjectsView'
        type: array
      error: {}
    type: object
  area.AreaWithProjectsView:
    properties:
      color:
        type: string
      createdAt:
        type: string
      displayOrder:
        type: integer
      icon:
        type: string
      id:
        type: string
      name:
        type: string
      projects:
        items:
          $ref: '#/definitions/project.ProjectView'
        type: array
      updatedAt:
        type: string
    type: object
  area.CreateAreaRequest:
    properties:
      color:
        type: string
      icon:
        type: string
      name:
        type: string
    type: object
  area.ErrorEnvelope:
    properties:
      data: {}
      error:
        $ref: '#/definitions/area.SwaggerError'
    type: object
  area.ListAreasResponse:
    properties:
      items:
        items:
          $ref: '#/definitions/area.AreaView'
        type: array
      nextCursor:
        type: string
    type: object
  area.ReorderItem:
    properties:
      displayOrder:
        type: integer
      id:
        type: string
    type: object
  area.ReorderRequest:
    properties:
      items:
        items:
          $ref: '#/definitions/area.ReorderItem'
        type: array
    type: object
  area.ReorderResult:
    properties:
      updated:
        example: 3
        type: integer
    type: object
  area.ReorderResultEnvelope:
    properties:
      data:
        $ref: '#/definitions/area.ReorderResult'
      error: {}
    type: object
  area.SwaggerError:
    properties:
      code:
        example: AREA_NOT_FOUND
        type: string
      message:
        example: area not found
        type: string
    type: object
  area.UpdateAreaRequest:
    properties:
      color:
        type: string
      icon:
        type: string
      name:
        type: string
    type: object
  auth.AuthEnvelope:
    properties:
      data:
        $ref: '#/definitions/auth.AuthResponse'
      error: {}
    type: object
  auth.AuthResponse:
    properties:
      refreshToken:
        type: string
      token:
        type: string
      user:
        $ref: '#/definitions/auth.UserView'
    type: object
  auth.CalendarPrefsView:
    properties:
      dayEndHour:
        type: integer
      dayStartHour:
        type: integer
      weekStart:
        type: integer
      workdays:
        items:
          type: integer
        type: array
    required:
    - dayEndHour
    - dayStartHour
    - weekStart
    - workdays
    type: object
  auth.ChangePasswordRequest:
    properties:
      confirmPassword:
        type: string
      currentPassword:
        type: string
      newPassword:
        type: string
    type: object
  auth.ErrorEnvelope:
    properties:
      data: {}
      error:
        $ref: '#/definitions/auth.SwaggerError'
    type: object
  auth.ForgotPasswordRequest:
    properties:
      email:
        type: string
    type: object
  auth.LoginRequest:
    properties:
      email:
        type: string
      identifier:
        type: string
      password:
        type: string
      platform:
        type: string
      remember:
        type: boolean
      timezone:
        type: string
    type: object
  auth.MessageData:
    properties:
      message:
        example: password updated successfully
        type: string
    type: object
  auth.MessageEnvelope:
    properties:
      data:
        $ref: '#/definitions/auth.MessageData'
      error: {}
    type: object
  auth.RegisterRequest:
    properties:
      email:
        type: string
      password:
        type: string
      platform:
        type: string
      username:
        type: string
    type: object
  auth.ResendVerificationRequest:
    properties:
      email:
        type: string
    type: object
  auth.ResetPasswordRequest:
    properties:
      confirmPassword:
        type: string
      newPassword:
        type: string
      token:
        type: string
    type: object
  auth.SwaggerError:
    properties:
      code:
        example: UNAUTHORIZED
        type: string
      message:
        example: invalid email or password
        type: string
    type: object
  auth.UpdateMeRequest:
    properties:
      dayEndHour:
        type: integer
      dayStartHour:
        type: integer
      firstName:
        type: string
      language:
        type: string
      lastName:
        type: string
      theme:
        type: string
      timezone:
        type: string
      weekStart:
        description: |-
          Calendar preferences are individually optional too, so a client changing
          only the day window never has to echo back a week_start it did not read.
        type: integer
      workdays:
        items:
          type: integer
        type: array
    type: object
  auth.UserEnvelope:
    properties:
      data:
        $ref: '#/definitions/auth.UserView'
      error: {}
    type: object
  auth.UserView:
    properties:
      calendar:
        allOf:
        - $ref: '#/definitions/auth.CalendarPrefsView'
        description: |-
          Calendar always travels with the user: the grid needs it on first paint,
          and a second round trip would render one frame of the wrong week.
      email:
        type: string
      firstName:
        type: string
      id:
        type: string
      imageUrl:
        type: string
      language:
        type: string
      lastName:
        type: string
      status:
        type: string
      theme:
        type: string
      timezone:
        type: string
      username:
        type: string
    type: object
  auth.VerifyEmailRequest:
    properties:
      token:
        type: string
    type: object
  bucket.BucketListResponse:
    properties:
      items:
        items:
          $ref: '#/definitions/bucket.BucketView'
        type: array
    type: object
  bucket.BucketView:
    properties:
      content:
        type: string
      createdAt:
        type: string
      createdNoteId:
        type: string
      createdTaskId:
        type: string
      id:
        type: string
      processedAt:
        type: string
      processingResult:
        type: string
      projectId:
        type: string
      updatedAt:
        type: string
    type: object
  bucket.CreateBucketRequest:
    properties:
      content:
        type: string
    type: object
  bucket.ProcessBucketRequest:
    properties:
      noteDetails:
        $ref: '#/definitions/bucket.ProcessNoteDetails'
      processingResult:
        type: string
      projectId:
        type: string
      taskDetails:
        $ref: '#/definitions/bucket.ProcessTaskDetails'
    type: object
  bucket.ProcessNoteDetails:
    properties:
      content:
        type: object
      title:
        type: string
    type: object
  bucket.ProcessRecurrenceDetails:
    properties:
      byMonthday:
        type: integer
      byWeekday:
        items:
          type: integer
        type: array
      endDate:
        type: string
      freq:
        type: string
      interval:
        type: integer
      startDate:
        type: string
    type: object
  bucket.ProcessTaskDetails:
    properties:
      energy:
        type: string
      estimatedMinutes:
        type: integer
      notes:
        type: string
      priority:
        type: string
      recurrence:
        $ref: '#/definitions/bucket.ProcessRecurrenceDetails'
      rollsOver:
        type: boolean
      scheduledFor:
        type: string
      scheduledTime:
        type: string
      title:
        type: string
      url:
        type: string
    type: object
  bucket.UpdateBucketRequest:
    properties:
      content:
        type: string
    type: object
  googlecal.CalendarView:
    properties:
      backgroundColor:
        type: string
      id:
        type: string
      primary:
        type: boolean
      selected:
        type: boolean
      summary:
        type: string
    required:
    - backgroundColor
    - id
    - primary
    - selected
    - summary
    type: object
  googlecal.ErrorEnvelope:
    properties:
      data: {}
      error:
        $ref: '#/definitions/googlecal.SwaggerError'
    type: object
  googlecal.EventsResponse:
    properties:
      events:
        items:
          $ref: '#/definitions/googlecal.GoogleEventView'
        type: array
      googleStatus:
        $ref: '#/definitions/googlecal.GoogleStatus'
    type: object
  googlecal.GoogleEventView:
    properties:
      allDay:
        type: boolean
      attendeeCount:
        type: integer
      calendarId:
        type: string
      description:
        type: string
      end:
        type: string
      htmlLink:
        type: string
      id:
        type: string
      location:
        type: string
      organizer:
        type: string
      responseStatus:
        $ref: '#/definitions/googlecal.ResponseStatus'
      start:
        type: string
      title:
        type: string
    type: object
  googlecal.GoogleStatus:
    enum:
    - ok
    - disconnected
    - error
    type: string
    x-enum-varnames:
    - StatusOK
    - StatusDisconnected
    - StatusError
  googlecal.ResponseStatus:
    enum:
    - ""
    - accepted
    - declined
    - tentative
    - needsAction
    type: string
    x-enum-varnames:
    - ResponseNone
    - ResponseAccepted
    - ResponseDeclined
    - ResponseTentative
    - ResponseNeedsAction
  googlecal.SwaggerError:
    properties:
      code:
        example: GOOGLE_NOT_CONNECTED
        type: string
      message:
        example: google calendar is not connected
        type: string
    type: object
  googlecal.UpdateSelectionRequest:
    properties:
      calendarIds:
        items:
          type: string
        type: array
    type: object
  habit.CellView:
    properties:
      date:
        type: string
      progress:
        allOf:
        - $ref: '#/definitions/habit.PeriodProgress'
        description: Set only on week cells, where a period accumulates toward a quota.
      satisfied:
        type: boolean
      scheduled:
        type: boolean
      value:
        type: integer
    type: object
  habit.CheckInRequest:
    properties:
      date:
        type: string
      value:
        type: integer
    type: object
  habit.CreateHabitRequest:
    properties:
      byWeekday:
        items:
          type: integer
        type: array
      color:
        type: string
      name:
        type: string
      polarity:
        type: string
      scheduleKind:
        type: string
      subject:
        type: string
      targetValue:
        type: integer
      timesPerWeek:
        type: integer
      unit:
        type: string
    type: object
  habit.ErrorEnvelope:
    properties:
      data: {}
      error:
        $ref: '#/definitions/habit.SwaggerError'
    type: object
  habit.HabitDetailEnvelope:
    properties:
      data:
        $ref: '#/definitions/habit.HabitView'
      error: {}
    type: object
  habit.HabitEnvelope:
    properties:
      data:
        $ref: '#/definitions/habit.HabitView'
      error: {}
    type: object
  habit.HabitListEnvelope:
    properties:
      data:
        items:
          $ref: '#/definitions/habit.HabitView'
        type: array
      error: {}
    type: object
  habit.HabitView:
    properties:
      archivedAt:
        type: string
      byWeekday:
        items:
          type: integer
        type: array
      cells:
        description: |-
          Cells is the heatmap window. A list read carries ListRibbonDays of it and
          a scalar read RibbonDays — same field, different width, so a board and a
          detail page render the same component against the same shape.

          Optional rather than required: it is omitted wherever there is no window
          to show, and a client that predates it simply renders no ribbon instead of
          breaking. Same contract style as periodProgress.
        items:
          $ref: '#/definitions/habit.CellView'
        type: array
      color:
        type: string
      completedToday:
        type: boolean
      createdAt:
        type: string
      currentStreak:
        type: integer
      dueToday:
        type: boolean
      id:
        type: string
      loggedToday:
        description: |-
          LoggedToday is whether an entry exists for today — NOT whether the period
          is satisfied. The two diverge on a quota habit: at 1 of 3 the week is
          unmet (CompletedToday false) but today is logged, and a control that
          keys off CompletedToday would check in again rather than undo.
        type: boolean
      longestStreak:
        type: integer
      name:
        type: string
      periodProgress:
        allOf:
        - $ref: '#/definitions/habit.PeriodProgress'
        description: |-
          PeriodProgress is the quota habit's "2 of 3 this week". Null for day
          habits — the client must not read a missing value as 0/0.
      polarity:
        type: string
      scheduleKind:
        type: string
      streakUnit:
        description: |-
          StreakUnit tells the client which noun to print ("12 days" vs "5 weeks")
          and which heatmap granularity to draw. Normalising everything to weeks was
          rejected: it would show "0 weeks" to someone on a 6-day run, which kills
          the daily feedback the feature is built on.
        type: string
      subject:
        type: string
      targetValue:
        type: integer
      timesPerWeek:
        type: integer
      todayValue:
        type: integer
      unit:
        type: string
      updatedAt:
        type: string
    type: object
  habit.PeriodProgress:
    properties:
      current:
        type: integer
      target:
        type: integer
    type: object
  habit.SubjectListEnvelope:
    properties:
      data:
        items:
          $ref: '#/definitions/habit.SubjectView'
        type: array
      error: {}
    type: object
  habit.SubjectView:
    properties:
      labelKey:
        type: string
      slug:
        type: string
    required:
    - labelKey
    - slug
    type: object
  habit.SwaggerError:
    properties:
      code:
        example: HABIT_NOT_FOUND
        type: string
      message:
        example: habit not found
        type: string
    type: object
  habit.UndoCheckInRequest:
    properties:
      date:
        type: string
    type: object
  habit.UpdateHabitRequest:
    properties:
      archived:
        type: boolean
      byWeekday:
        items:
          type: integer
        type: array
      color:
        type: string
      name:
        type: string
      scheduleKind:
        type: string
      subject:
        type: string
      targetValue:
        type: integer
      timesPerWeek:
        type: integer
      unit:
        type: string
    type: object
  note.CreateNoteRequest:
    properties:
      content:
        type: object
      projectId:
        type: string
      title:
        type: string
    type: object
  note.ErrorEnvelope:
    properties:
      data: {}
      error:
        $ref: '#/definitions/note.SwaggerError'
    type: object
  note.MentionResult:
    properties:
      id:
        type: string
      title:
        type: string
    type: object
  note.MentionSearchEnvelope:
    properties:
      data:
        items:
          $ref: '#/definitions/note.MentionResult'
        type: array
      error: {}
    type: object
  note.NoteDetailEnvelope:
    properties:
      data:
        $ref: '#/definitions/note.NoteDetailView'
      error: {}
    type: object
  note.NoteDetailView:
    properties:
      content:
        type: object
      createdAt:
        type: string
      id:
        type: string
      projectId:
        type: string
      title:
        type: string
      updatedAt:
        type: string
      version:
        type: integer
    type: object
  note.NoteListEnvelope:
    properties:
      data:
        items:
          $ref: '#/definitions/note.NoteView'
        type: array
      error: {}
    type: object
  note.NoteView:
    properties:
      createdAt:
        type: string
      excerpt:
        type: string
      id:
        type: string
      projectId:
        type: string
      title:
        type: string
      updatedAt:
        type: string
      version:
        type: integer
    type: object
  note.SwaggerError:
    properties:
      code:
        example: RESOURCE_NOT_FOUND
        type: string
      message:
        example: note not found
        type: string
    type: object
  note.UpdateNoteRequest:
    properties:
      content:
        type: object
      title:
        type: string
      version:
        type: integer
    type: object
  notification.CountEnvelope:
    properties:
      data:
        $ref: '#/definitions/notification.CountResponse'
      error: {}
    type: object
  notification.CountResponse:
    properties:
      count:
        type: integer
    type: object
  notification.ErrorEnvelope:
    properties:
      data: {}
      error:
        $ref: '#/definitions/notification.SwaggerError'
    type: object
  notification.ListNotificationsResponse:
    properties:
      items:
        items:
          $ref: '#/definitions/notification.NotificationView'
        type: array
      nextCursor:
        type: string
    type: object
  notification.NotificationEnvelope:
    properties:
      data:
        $ref: '#/definitions/notification.NotificationView'
      error: {}
    type: object
  notification.NotificationListEnvelope:
    properties:
      data:
        $ref: '#/definitions/notification.ListNotificationsResponse'
      error: {}
    type: object
  notification.NotificationView:
    properties:
      body:
        type: string
      category:
        type: string
      createdAt:
        type: string
      id:
        type: string
      isRead:
        type: boolean
      metadata:
        type: object
      readAt:
        type: string
      title:
        type: string
      type:
        type: string
    type: object
  notification.PreferencesEnvelope:
    properties:
      data:
        $ref: '#/definitions/notification.PreferencesView'
      error: {}
    type: object
  notification.PreferencesView:
    properties:
      emailDigest:
        type: boolean
      eveningDigestEnabled:
        type: boolean
      eveningHour:
        type: integer
      morningDigestEnabled:
        type: boolean
      morningHour:
        type: integer
      pushEnabled:
        type: boolean
      smsEnabled:
        type: boolean
      streaksEnabled:
        type: boolean
    type: object
  notification.SubscribeRequest:
    properties:
      authKey:
        type: string
      endpoint:
        type: string
      p256dhKey:
        type: string
      userAgent:
        type: string
    type: object
  notification.SwaggerError:
    properties:
      code:
        example: NOTIFICATION_NOT_FOUND
        type: string
      message:
        example: notification not found
        type: string
    type: object
  notification.UnreadCountEnvelope:
    properties:
      data:
        $ref: '#/definitions/notification.UnreadCountResponse'
      error: {}
    type: object
  notification.UnreadCountResponse:
    properties:
      count:
        type: integer
    type: object
  notification.UpdatePreferences:
    properties:
      emailDigest:
        type: boolean
      eveningDigestEnabled:
        type: boolean
      eveningHour:
        type: integer
      morningDigestEnabled:
        type: boolean
      morningHour:
        type: integer
      pushEnabled:
        type: boolean
      smsEnabled:
        type: boolean
      streaksEnabled:
        type: boolean
    type: object
  project.CreateProjectRequest:
    properties:
      description:
        type: string
      dueDate:
        type: string
      folderIcon:
        type: string
      isFavorite:
        type: boolean
      name:
        type: string
      status:
        type: string
    type: object
  project.ErrorEnvelope:
    properties:
      data: {}
      error:
        $ref: '#/definitions/project.SwaggerError'
    type: object
  project.ListProjectsResponse:
    properties:
      items:
        items:
          $ref: '#/definitions/project.ProjectView'
        type: array
      nextCursor:
        type: string
    type: object
  project.ProjectEnvelope:
    properties:
      data:
        $ref: '#/definitions/project.ProjectView'
      error: {}
    type: object
  project.ProjectListEnvelope:
    properties:
      data:
        $ref: '#/definitions/project.ListProjectsResponse'
      error: {}
    type: object
  project.ProjectView:
    properties:
      areaId:
        type: string
      createdAt:
        type: string
      description:
        type: string
      displayOrder:
        type: integer
      dueDate:
        type: string
      folderIcon:
        type: string
      id:
        type: string
      isFavorite:
        type: boolean
      name:
        type: string
      status:
        type: string
      updatedAt:
        type: string
    type: object
  project.ReorderItem:
    properties:
      displayOrder:
        type: integer
      id:
        type: string
    type: object
  project.ReorderRequest:
    properties:
      items:
        items:
          $ref: '#/definitions/project.ReorderItem'
        type: array
    type: object
  project.ReorderResult:
    properties:
      updated:
        example: 3
        type: integer
    type: object
  project.ReorderResultEnvelope:
    properties:
      data:
        $ref: '#/definitions/project.ReorderResult'
      error: {}
    type: object
  project.SwaggerError:
    properties:
      code:
        example: PROJECT_NOT_FOUND
        type: string
      message:
        example: project not found
        type: string
    type: object
  project.UpdateProjectRequest:
    type: object
  search.AreaResult:
    properties:
      id:
        type: string
      name:
        type: string
    type: object
  search.ErrorEnvelope:
    properties:
      data: {}
      error:
        $ref: '#/definitions/search.SwaggerError'
    type: object
  search.NoteResult:
    properties:
      excerpt:
        type: string
      id:
        type: string
      projectId:
        type: string
      projectName:
        type: string
      title:
        type: string
    type: object
  search.ProjectResult:
    properties:
      areaName:
        type: string
      id:
        type: string
      name:
        type: string
    type: object
  search.Response:
    properties:
      areas:
        items:
          $ref: '#/definitions/search.AreaResult'
        type: array
      notes:
        items:
          $ref: '#/definitions/search.NoteResult'
        type: array
      projects:
        items:
          $ref: '#/definitions/search.ProjectResult'
        type: array
      tasks:
        items:
          $ref: '#/definitions/search.TaskResult'
        type: array
    type: object
  search.SearchEnvelope:
    properties:
      data:
        $ref: '#/definitions/search.Response'
      error: {}
    type: object
  search.SwaggerError:
    properties:
      code:
        example: INVALID_INPUT
        type: string
      message:
        example: query is required
        type: string
    type: object
  search.TaskResult:
    properties:
      excerpt:
        type: string
      id:
        type: string
      projectId:
        type: string
      projectName:
        type: string
      title:
        type: string
    type: object
  task.CreateSubtaskRequest:
    properties:
      position:
        type: integer
      title:
        type: string
    type: object
  task.CreateTaskRequest:
    properties:
      energy:
        type: string
      estimatedMinutes:
        type: integer
      notes:
        type: string
      priority:
        type: string
      rollsOver:
        type: boolean
      scheduledFor:
        type: string
      scheduledTime:
        type: string
      status:
        type: string
      title:
        type: string
      url:
        type: string
    type: object
  task.ErrorEnvelope:
    properties:
      data: {}
      error:
        $ref: '#/definitions/task.SwaggerError'
    type: object
  task.ListSubtasksResponse:
    properties:
      items:
        items:
          $ref: '#/definitions/task.SubtaskView'
        type: array
    type: object
  task.ListTasksResponse:
    properties:
      items:
        items:
          $ref: '#/definitions/task.TaskView'
        type: array
      nextCursor:
        type: string
    type: object
  task.ReorderOneRequest:
    properties:
      displayOrder:
        type: integer
    type: object
  task.ScheduleRequest:
    properties:
      rollsOver:
        type: boolean
      scheduledFor:
        type: string
      scheduledTime:
        type: string
    type: object
  task.SetStatusRequest:
    properties:
      status:
        type: string
    type: object
  task.SubtaskEnvelope:
    properties:
      data:
        $ref: '#/definitions/task.SubtaskView'
      error: {}
    type: object
  task.SubtaskListEnvelope:
    properties:
      data:
        $ref: '#/definitions/task.ListSubtasksResponse'
      error: {}
    type: object
  task.SubtaskView:
    properties:
      createdAt:
        type: string
      done:
        type: boolean
      id:
        type: string
      position:
        type: integer
      taskId:
        type: string
      title:
        type: string
      updatedAt:
        type: string
    type: object
  task.SwaggerError:
    properties:
      code:
        example: TASK_NOT_FOUND
        type: string
      message:
        example: task not found
        type: string
    type: object
  task.TaskEnvelope:
    properties:
      data:
        $ref: '#/definitions/task.TaskView'
      error: {}
    type: object
  task.TaskListEnvelope:
    properties:
      data:
        $ref: '#/definitions/task.ListTasksResponse'
      error: {}
    type: object
  task.TaskView:
    properties:
      completedAt:
        type: string
      createdAt:
        type: string
      displayOrder:
        type: integer
      energy:
        type: string
      estimatedMinutes:
        type: integer
      id:
        type: string
      notes:
        type: string
      occurrenceDate:
        type: string
      occurrenceStatus:
        type: string
      openSubtaskCount:
        type: integer
      priority:
        type: string
      projectId:
        type: string
      recurrenceRuleId:
        type: string
      rollsOver:
        type: boolean
      scheduledFor:
        type: string
      scheduledTime:
        type: string
      status:
        type: string
      subtaskCount:
        description: SubtaskCount / OpenSubtaskCount are always populated, on every
          read.
        type: integer
      title:
        type: string
      totalFocusSeconds:
        description: |-
          TotalFocusSeconds is the SUM of the task's closed focus segments (E-049).
          Enriched only on Focus + GetTask; 0 on the project task-list, where a
          per-row SUM would be pure cost for a value the list never renders.
        type: integer
      updatedAt:
        type: string
      url:
        type: string
    type: object
  task.TimeSpreadEnvelope:
    properties:
      data:
        $ref: '#/definitions/task.TimeSpreadResponse'
      error: {}
    type: object
  task.TimeSpreadResponse:
    properties:
      thisWeek:
        items:
          $ref: '#/definitions/task.TaskView'
        type: array
      today:
        items:
          $ref: '#/definitions/task.TaskView'
        type: array
      tomorrow:
        items:
          $ref: '#/definitions/task.TaskView'
        type: array
    type: object
  task.UpdateSubtaskRequest:
    properties:
      done:
        type: boolean
      position:
        type: integer
      title:
        type: string
    type: object
  task.UpdateTaskRequest:
    type: object
info:
  contact: {}
  description: REST API for the Nicoflow GTD task-management platform. Covers authentication
    & user management, Areas, Projects, Tasks (CRUD, quick actions, filter/sort/search),
    Subtasks, Focus and Time-Spread.
  title: Nicoflow API
  version: "1.0"
paths:
  /areas:
    get:
      description: Lists the user's areas, cursor-paginated, with optional name search.
      parameters:
      - description: Search by area name
        in: query
        name: q
        type: string
      - description: Page size (1–100)
        in: query
        name: limit
        type: integer
      - description: Pagination cursor
        in: query
        name: cursor
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: Paginated areas
          schema:
            $ref: '#/definitions/area.AreaListEnvelope'
        "400":
          description: INVALID_INPUT (bad limit/cursor)
          schema:
            $ref: '#/definitions/area.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: List areas
      tags:
      - areas
    post:
      consumes:
      - application/json
      description: Creates an area. Free plan allows up to 3 areas.
      parameters:
      - description: Area to create
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/area.CreateAreaRequest'
      produces:
      - application/json
      responses:
        "201":
          description: The created area
          schema:
            $ref: '#/definitions/area.AreaEnvelope'
        "403":
          description: PLAN_LIMIT_EXCEEDED
          schema:
            $ref: '#/definitions/area.ErrorEnvelope'
        "409":
          description: DUPLICATE_NAME
          schema:
            $ref: '#/definitions/area.ErrorEnvelope'
        "422":
          description: INVALID_INPUT
          schema:
            $ref: '#/definitions/area.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Create an area
      tags:
      - areas
  /areas/{areaId}/projects:
    get:
      description: Lists the projects within one area, cursor-paginated, with optional
        filters.
      parameters:
      - description: Area ID
        in: path
        name: areaId
        required: true
        type: string
      - description: Search by project name
        in: query
        name: q
        type: string
      - description: Filter by status (active|completed|archived)
        in: query
        name: status
        type: string
      - description: Filter by favorite
        in: query
        name: isFavorite
        type: boolean
      - description: Page size (1–100)
        in: query
        name: limit
        type: integer
      - description: Pagination cursor
        in: query
        name: cursor
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: Paginated projects
          schema:
            $ref: '#/definitions/project.ProjectListEnvelope'
        "400":
          description: INVALID_INPUT
          schema:
            $ref: '#/definitions/project.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: List projects in an area
      tags:
      - projects
    post:
      consumes:
      - application/json
      description: Creates a project inside an area. Free plan allows up to 5 projects
        total.
      parameters:
      - description: Area ID
        in: path
        name: areaId
        required: true
        type: string
      - description: Project to create
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/project.CreateProjectRequest'
      produces:
      - application/json
      responses:
        "201":
          description: The created project
          schema:
            $ref: '#/definitions/project.ProjectEnvelope'
        "403":
          description: PLAN_LIMIT_EXCEEDED
          schema:
            $ref: '#/definitions/project.ErrorEnvelope'
        "404":
          description: AREA_NOT_FOUND
          schema:
            $ref: '#/definitions/project.ErrorEnvelope'
        "409":
          description: DUPLICATE_NAME
          schema:
            $ref: '#/definitions/project.ErrorEnvelope'
        "422":
          description: INVALID_INPUT
          schema:
            $ref: '#/definitions/project.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Create a project
      tags:
      - projects
  /areas/{id}:
    delete:
      description: Deletes an area; its projects are cascade-deleted (their tasks
        survive unfiled).
      parameters:
      - description: Area ID
        in: path
        name: id
        required: true
        type: string
      responses:
        "204":
          description: No Content
        "404":
          description: AREA_NOT_FOUND
          schema:
            $ref: '#/definitions/area.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Delete an area
      tags:
      - areas
    get:
      description: Retrieves a single area by ID. Cross-user access returns 404.
      parameters:
      - description: Area ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: The area
          schema:
            $ref: '#/definitions/area.AreaEnvelope'
        "404":
          description: AREA_NOT_FOUND
          schema:
            $ref: '#/definitions/area.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Get an area
      tags:
      - areas
    patch:
      consumes:
      - application/json
      description: Partial update of an area (name, color, icon).
      parameters:
      - description: Area ID
        in: path
        name: id
        required: true
        type: string
      - description: Fields to update (all optional)
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/area.UpdateAreaRequest'
      produces:
      - application/json
      responses:
        "200":
          description: The updated area
          schema:
            $ref: '#/definitions/area.AreaEnvelope'
        "404":
          description: AREA_NOT_FOUND
          schema:
            $ref: '#/definitions/area.ErrorEnvelope'
        "409":
          description: DUPLICATE_NAME
          schema:
            $ref: '#/definitions/area.ErrorEnvelope'
        "422":
          description: INVALID_INPUT
          schema:
            $ref: '#/definitions/area.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Update an area
      tags:
      - areas
  /areas/reorder:
    patch:
      consumes:
      - application/json
      description: Bulk-updates area displayOrder in one transaction (atomic; a foreign
        id rolls the whole batch back).
      parameters:
      - description: New order (id → displayOrder)
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/area.ReorderRequest'
      produces:
      - application/json
      responses:
        "200":
          description: Number of rows updated
          schema:
            $ref: '#/definitions/area.ReorderResultEnvelope'
        "404":
          description: AREA_NOT_FOUND (a foreign id — whole batch rolled back)
          schema:
            $ref: '#/definitions/area.ErrorEnvelope'
        "422":
          description: INVALID_INPUT
          schema:
            $ref: '#/definitions/area.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Reorder areas
      tags:
      - areas
  /areas/with-projects:
    get:
      description: Returns every area with its projects nested — the payload the board
        renders from.
      produces:
      - application/json
      responses:
        "200":
          description: Areas with nested projects
          schema:
            $ref: '#/definitions/area.AreaWithProjectsEnvelope'
      security:
      - BearerAuth: []
      summary: List areas with nested projects
      tags:
      - areas
  /auth/change-password:
    post:
      consumes:
      - application/json
      description: Changes the authenticated user's password. Re-verifies currentPassword
        via bcrypt, enforces the register password policy on newPassword, revokes
        all refresh tokens, and issues a fresh token pair to the caller (every other
        device is signed out; the caller stays signed in). Sets a new HttpOnly refresh
        cookie.
      parameters:
      - description: Current + new password
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/auth.ChangePasswordRequest'
      produces:
      - application/json
      responses:
        "200":
          description: New access token + rotated refresh cookie
          schema:
            $ref: '#/definitions/auth.AuthEnvelope'
        "400":
          description: WEAK_PASSWORD
          schema:
            $ref: '#/definitions/auth.ErrorEnvelope'
        "401":
          description: UNAUTHORIZED (missing access token or wrong current password)
          schema:
            $ref: '#/definitions/auth.ErrorEnvelope'
        "422":
          description: INVALID_INPUT (password mismatch)
          schema:
            $ref: '#/definitions/auth.ErrorEnvelope'
        "429":
          description: RATE_LIMITED
          schema:
            $ref: '#/definitions/auth.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Change password (logged in)
      tags:
      - auth
  /auth/forgot-password:
    post:
      consumes:
      - application/json
      description: Sends a password-reset email. Always returns 200 regardless of
        whether the email exists (no user enumeration).
      parameters:
      - description: Account email
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/auth.ForgotPasswordRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/auth.MessageEnvelope'
        "429":
          description: RATE_LIMITED
          schema:
            $ref: '#/definitions/auth.ErrorEnvelope'
      summary: Request a password reset
      tags:
      - auth
  /auth/login:
    post:
      consumes:
      - application/json
      description: Authenticates by email or username and returns a JWT access token
        plus an HttpOnly refresh cookie. When REQUIRE_EMAIL_VERIFICATION is enabled,
        an unverified account is rejected with 403 EMAIL_NOT_VERIFIED after the password
        check (so verification state is never leaked to a wrong password).
      parameters:
      - description: Credentials (identifier = email or username)
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/auth.LoginRequest'
      produces:
      - application/json
      responses:
        "200":
          description: Access token + user; Set-Cookie refresh token
          schema:
            $ref: '#/definitions/auth.AuthEnvelope'
        "401":
          description: UNAUTHORIZED (invalid credentials; identical for unknown user
            and wrong password)
          schema:
            $ref: '#/definitions/auth.ErrorEnvelope'
        "403":
          description: EMAIL_NOT_VERIFIED
          schema:
            $ref: '#/definitions/auth.ErrorEnvelope'
        "422":
          description: INVALID_INPUT
          schema:
            $ref: '#/definitions/auth.ErrorEnvelope'
        "429":
          description: RATE_LIMITED (IP limit or account lockout)
          schema:
            $ref: '#/definitions/auth.ErrorEnvelope'
      summary: Log in
      tags:
      - auth
  /auth/logout:
    post:
      description: Revokes the single refresh token carried by the cookie and clears
        it. Authenticates off the HttpOnly refresh cookie, not the access token. Idempotent
        — a missing or already-deleted token still returns 204.
      produces:
      - application/json
      responses:
        "204":
          description: Session ended; refresh cookie cleared
      summary: Log out (current session)
      tags:
      - auth
  /auth/logout-all:
    post:
      description: Revokes every refresh token for the authenticated user. Requires
        a valid access token (revokes by the userID claim).
      produces:
      - application/json
      responses:
        "204":
          description: All sessions revoked; refresh cookie cleared
        "401":
          description: UNAUTHORIZED (missing or invalid access token)
          schema:
            $ref: '#/definitions/auth.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Log out of all devices
      tags:
      - auth
  /auth/refresh-token:
    post:
      consumes:
      - application/json
      description: Rotates the refresh token (delete-old/insert-new) and issues a
        fresh access token. The raw refresh token is read from the HttpOnly cookie
        (preferred) or a JSON body. Replaying a consumed token is reuse and revokes
        all of the user's sessions.
      produces:
      - application/json
      responses:
        "200":
          description: New access token + rotated refresh cookie
          schema:
            $ref: '#/definitions/auth.AuthEnvelope'
        "401":
          description: INVALID_TOKEN (missing, expired, tampered, or already-used)
          schema:
            $ref: '#/definitions/auth.ErrorEnvelope'
      summary: Refresh the access token
      tags:
      - auth
  /auth/register:
    post:
      consumes:
      - application/json
      description: Creates a user and sends a verification email. Does not log the
        user in — no token is issued and no cookie is set; the user must verify (when
        enforced) then log in.
      parameters:
      - description: Registration details
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/auth.RegisterRequest'
      produces:
      - application/json
      responses:
        "201":
          description: User created (token empty, no session)
          schema:
            $ref: '#/definitions/auth.AuthEnvelope'
        "400":
          description: WEAK_PASSWORD
          schema:
            $ref: '#/definitions/auth.ErrorEnvelope'
        "409":
          description: EMAIL_ALREADY_EXISTS or USERNAME_ALREADY_EXISTS
          schema:
            $ref: '#/definitions/auth.ErrorEnvelope'
        "422":
          description: INVALID_INPUT or INVALID_EMAIL
          schema:
            $ref: '#/definitions/auth.ErrorEnvelope'
        "429":
          description: RATE_LIMITED
          schema:
            $ref: '#/definitions/auth.ErrorEnvelope'
      summary: Register a new account
      tags:
      - auth
  /auth/resend-verification:
    post:
      consumes:
      - application/json
      description: Re-sends the email-verification link. Always returns 200 (no user
        enumeration).
      parameters:
      - description: Account email
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/auth.ResendVerificationRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/auth.MessageEnvelope'
        "429":
          description: RATE_LIMITED
          schema:
            $ref: '#/definitions/auth.ErrorEnvelope'
      summary: Resend the verification email
      tags:
      - auth
  /auth/reset-password:
    post:
      consumes:
      - application/json
      description: Sets a new password using the single-use reset token from the email.
        Revokes all active refresh tokens on success.
      parameters:
      - description: Reset token + new password
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/auth.ResetPasswordRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/auth.MessageEnvelope'
        "400":
          description: WEAK_PASSWORD
          schema:
            $ref: '#/definitions/auth.ErrorEnvelope'
        "401":
          description: INVALID_TOKEN (invalid, expired, or already-used)
          schema:
            $ref: '#/definitions/auth.ErrorEnvelope'
        "422":
          description: INVALID_INPUT (e.g. password mismatch)
          schema:
            $ref: '#/definitions/auth.ErrorEnvelope'
      summary: Reset password with a token
      tags:
      - auth
  /auth/verify-email:
    post:
      consumes:
      - application/json
      description: Confirms a user's email using the single-use token from the verification
        email.
      parameters:
      - description: Verification token
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/auth.VerifyEmailRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/auth.MessageEnvelope'
        "401":
          description: INVALID_TOKEN (invalid, expired, or already-used)
          schema:
            $ref: '#/definitions/auth.ErrorEnvelope'
        "422":
          description: INVALID_INPUT
          schema:
            $ref: '#/definitions/auth.ErrorEnvelope'
      summary: Verify email address
      tags:
      - auth
  /bucket:
    get:
      description: Lists the caller's bucket items, newest first. Processed and unprocessed
        items are both returned; the client partitions them into Inbox / Archived.
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/bucket.BucketListResponse'
      security:
      - BearerAuth: []
      summary: List inbox items
      tags:
      - bucket
    post:
      consumes:
      - application/json
      description: Quick-captures a thought into the bucket. Content is 1–500 characters
        (trimmed).
      parameters:
      - description: Content to capture
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/bucket.CreateBucketRequest'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/bucket.BucketView'
        "422":
          description: INVALID_INPUT
      security:
      - BearerAuth: []
      summary: Capture an inbox item
      tags:
      - bucket
  /bucket/{id}:
    delete:
      description: Hard-deletes a bucket item regardless of processed state.
      parameters:
      - description: Bucket item ID
        in: path
        name: id
        required: true
        type: string
      responses:
        "204":
          description: No Content
        "404":
          description: RESOURCE_NOT_FOUND
      security:
      - BearerAuth: []
      summary: Delete an inbox item
      tags:
      - bucket
    get:
      description: Retrieves a single bucket item by ID. Cross-user access returns
        404 (no existence leak).
      parameters:
      - description: Bucket item ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/bucket.BucketView'
        "404":
          description: RESOURCE_NOT_FOUND
      security:
      - BearerAuth: []
      summary: Get an inbox item
      tags:
      - bucket
    patch:
      consumes:
      - application/json
      description: Edits the content of an unprocessed bucket item. Editing a processed
        item returns 409.
      parameters:
      - description: Bucket item ID
        in: path
        name: id
        required: true
        type: string
      - description: New content
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/bucket.UpdateBucketRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/bucket.BucketView'
        "404":
          description: RESOURCE_NOT_FOUND
        "409":
          description: CONFLICT (already processed)
        "422":
          description: INVALID_INPUT
      security:
      - BearerAuth: []
      summary: Edit an unprocessed inbox item
      tags:
      - bucket
  /bucket/{id}/process:
    post:
      consumes:
      - application/json
      description: 'Converts an unprocessed item into a task (requires projectId +
        taskDetails) or note (requires projectId + noteDetails), or trashes it. taskDetails.scheduledTime
        and taskDetails.recurrence give the task path full parity with plain task
        creation — when recurrence is set, a recurrence rule is created (materializing
        instance #1) instead of a plain task. Already-processed items return 409.'
      parameters:
      - description: Bucket item ID
        in: path
        name: id
        required: true
        type: string
      - description: How to process the item
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/bucket.ProcessBucketRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/bucket.BucketView'
        "403":
          description: PLAN_LIMIT_EXCEEDED
        "404":
          description: RESOURCE_NOT_FOUND / PROJECT_NOT_FOUND
        "409":
          description: CONFLICT (already processed)
        "422":
          description: INVALID_INPUT
        "501":
          description: note processing not implemented
      security:
      - BearerAuth: []
      summary: Process an inbox item
      tags:
      - bucket
  /calendar/google-events:
    get:
      description: Read-only overlay of the user's selected Google calendars for an
        inclusive date range (max 62 days). Never returns 5xx for a Google-side failure
        — the response carries `googleStatus` (`ok`|`disconnected`|`error`) with an
        empty list instead, so a Google outage cannot break the calendar view. `refresh=true`
        bypasses the short server-side cache.
      parameters:
      - description: Inclusive start date (YYYY-MM-DD)
        in: query
        name: from
        required: true
        type: string
      - description: Inclusive end date (YYYY-MM-DD)
        in: query
        name: to
        required: true
        type: string
      - description: Bypass the cache and re-query Google
        in: query
        name: refresh
        type: boolean
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/googlecal.EventsResponse'
        "422":
          description: INVALID_INPUT (missing bound, bad date, or span > 62 days)
          schema:
            $ref: '#/definitions/googlecal.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: List Google Calendar events
      tags:
      - calendar
  /calendar/google/calendars:
    get:
      description: The calendars the user can read, each flagged with whether it currently
        overlays the Nicoflow calendar. A connection with no stored selection reports
        the primary calendar as selected, matching the overlay's default.
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            items:
              $ref: '#/definitions/googlecal.CalendarView'
            type: array
        "409":
          description: GOOGLE_NOT_CONNECTED
          schema:
            $ref: '#/definitions/googlecal.ErrorEnvelope'
        "502":
          description: GOOGLE_AUTH_FAILED — Google unreachable
          schema:
            $ref: '#/definitions/googlecal.ErrorEnvelope'
        "503":
          description: GOOGLE_AUTH_FAILED — integration not configured
          schema:
            $ref: '#/definitions/googlecal.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: List the user's Google calendars
      tags:
      - calendar
    put:
      consumes:
      - application/json
      description: Replaces the overlay selection. Capped at 5 calendars — each selected
        calendar is a separate Google API call per ranged fetch. Duplicates are collapsed
        rather than rejected. Returns the full calendar list with the new selection
        applied.
      parameters:
      - description: Calendar IDs to select
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/googlecal.UpdateSelectionRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            items:
              $ref: '#/definitions/googlecal.CalendarView'
            type: array
        "409":
          description: GOOGLE_NOT_CONNECTED
          schema:
            $ref: '#/definitions/googlecal.ErrorEnvelope'
        "422":
          description: INVALID_INPUT — missing field, empty ID, or more than 5 calendars
          schema:
            $ref: '#/definitions/googlecal.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Replace the selected calendars
      tags:
      - calendar
  /focus:
    get:
      description: 'Returns a deterministically-ranked short list of the user''s active
        tasks (across all projects) that fit the given time/energy. Scoring: energy
        match, time-budget fit (over-budget excluded), due/overdue escalation, scheduled
        proximity, priority tiebreak.'
      parameters:
      - description: Minutes available (over-budget tasks excluded)
        in: query
        name: available
        type: integer
      - description: Current energy (low|medium|deep)
        in: query
        name: energy
        type: string
      - description: Max results (default 5, max 20)
        in: query
        name: limit
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: Ranked tasks
          schema:
            $ref: '#/definitions/task.TaskListEnvelope'
        "400":
          description: INVALID_INPUT (bad available/energy/limit)
          schema:
            $ref: '#/definitions/task.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Focus — what can I do right now?
      tags:
      - tasks
  /habits:
    get:
      description: Returns the caller's habits, newest first, each with a short heatmap
        window (14 cells) alongside its derived counters — enough for a board to draw
        a ribbon per card without a follow-up request. Archived habits are excluded
        unless includeArchived=true. Free users may hold 3 active habits; a downgraded
        user keeps every habit visible and checkable, only creation is blocked.
      parameters:
      - description: Include archived habits
        in: query
        name: includeArchived
        type: boolean
      produces:
      - application/json
      responses:
        "200":
          description: Habits
          schema:
            $ref: '#/definitions/habit.HabitListEnvelope'
      security:
      - BearerAuth: []
      summary: List habits
      tags:
      - habits
    post:
      consumes:
      - application/json
      description: Creates a habit. scheduleKind is one of daily, weekdays (requires
        byWeekday) or weekly_quota (requires timesPerWeek). polarity is build or quit
        and is immutable afterwards. Free plan allows 3 active habits.
      parameters:
      - description: Habit to create
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/habit.CreateHabitRequest'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/habit.HabitEnvelope'
        "403":
          description: PLAN_LIMIT_EXCEEDED
          schema:
            $ref: '#/definitions/habit.ErrorEnvelope'
        "422":
          description: INVALID_INPUT
          schema:
            $ref: '#/definitions/habit.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Create a habit
      tags:
      - habits
  /habits/{id}:
    delete:
      description: 'Archives a habit by default: the row is retired, the check-in
        history is retained — it is the user''s record of what they did — and a plan
        slot is freed. Pass permanent=true to delete it outright instead, which cascades
        to every check-in and cannot be undone. The default is the reversible one
        on purpose. Cross-user access returns 404, never 403.'
      parameters:
      - description: Habit ID
        in: path
        name: id
        required: true
        type: string
      - description: Destroy the habit and its history instead of archiving it
        in: query
        name: permanent
        type: boolean
      produces:
      - application/json
      responses:
        "204":
          description: Archived, or deleted when permanent=true
        "404":
          description: HABIT_NOT_FOUND
          schema:
            $ref: '#/definitions/habit.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Archive or delete a habit
      tags:
      - habits
    get:
      description: Retrieves a single habit with its derived counters and the heatmap
        window behind them. Cells are day cells for daily/weekdays habits and week
        cells (carrying quota progress) for weekly_quota habits — the granularity
        streakUnit announces. Cross-user access returns 404, never 403.
      parameters:
      - description: Habit ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: The habit with its history
          schema:
            $ref: '#/definitions/habit.HabitDetailEnvelope'
        "404":
          description: HABIT_NOT_FOUND
          schema:
            $ref: '#/definitions/habit.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Get a habit
      tags:
      - habits
    patch:
      consumes:
      - application/json
      description: Edits a habit. polarity is immutable — sending it returns 422.
        Changing the schedule applies forward only; periods already scored keep their
        shape. Set archived to true to retire a habit (freeing a plan slot) or false
        to restore it.
      parameters:
      - description: Habit ID
        in: path
        name: id
        required: true
        type: string
      - description: Fields to change
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/habit.UpdateHabitRequest'
      produces:
      - application/json
      responses:
        "200":
          description: Updated
          schema:
            $ref: '#/definitions/habit.HabitEnvelope'
        "403":
          description: PLAN_LIMIT_EXCEEDED (restoring over the limit)
          schema:
            $ref: '#/definitions/habit.ErrorEnvelope'
        "404":
          description: HABIT_NOT_FOUND
          schema:
            $ref: '#/definitions/habit.ErrorEnvelope'
        "422":
          description: INVALID_INPUT (includes an attempted polarity change)
          schema:
            $ref: '#/definitions/habit.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Update a habit
      tags:
      - habits
  /habits/{id}/check-in:
    delete:
      consumes:
      - application/json
      description: 'Removes one dated entry, reverting the day to not-done. Omit date
        to undo today. Idempotent: undoing a date with no entry succeeds, because
        the day is already not done.'
      parameters:
      - description: Habit ID
        in: path
        name: id
        required: true
        type: string
      - description: Optional date
        in: body
        name: request
        schema:
          $ref: '#/definitions/habit.UndoCheckInRequest'
      produces:
      - application/json
      responses:
        "200":
          description: The habit
          schema:
            $ref: '#/definitions/habit.HabitEnvelope'
        "404":
          description: HABIT_NOT_FOUND
          schema:
            $ref: '#/definitions/habit.ErrorEnvelope'
        "422":
          description: INVALID_INPUT (future date, outside the backfill window, or
            archived habit)
          schema:
            $ref: '#/definitions/habit.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Undo a habit check-in
      tags:
      - habits
    post:
      consumes:
      - application/json
      description: 'Records one dated entry. Omit date to check in for today — the
        server resolves it from the user''s timezone and never accepts a client-supplied
        "today". Omit value to use the habit''s target. Idempotent per (habit, date):
        a repeat call updates the value. A past date is a backfill and must fall inside
        the window (7 days for daily/weekdays habits, the current and previous week
        for weekly quota); future dates are refused.'
      parameters:
      - description: Habit ID
        in: path
        name: id
        required: true
        type: string
      - description: Optional date and value
        in: body
        name: request
        schema:
          $ref: '#/definitions/habit.CheckInRequest'
      produces:
      - application/json
      responses:
        "200":
          description: The habit
          schema:
            $ref: '#/definitions/habit.HabitEnvelope'
        "404":
          description: HABIT_NOT_FOUND
          schema:
            $ref: '#/definitions/habit.ErrorEnvelope'
        "422":
          description: INVALID_INPUT (future date, outside the backfill window, negative
            value, or archived habit)
          schema:
            $ref: '#/definitions/habit.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Check in to a habit
      tags:
      - habits
  /habits/subjects:
    get:
      description: The canonical subject list. Subjects are cosmetic — they drive
        the card icon and never scheduling or targets. labelKey is an i18n key, not
        a display string. A client meeting an unknown slug renders a fallback icon
        rather than failing, so the catalog can gain entries without a client release.
      produces:
      - application/json
      responses:
        "200":
          description: Subjects
          schema:
            $ref: '#/definitions/habit.SubjectListEnvelope'
      security:
      - BearerAuth: []
      summary: Habit subject catalog
      tags:
      - habits
  /habits/today:
    get:
      description: Returns the habits still owed right now, for the Today-page strip.
        A weekdays habit appears only on its own days; a weekly-quota habit appears
        every day until its week's quota is met, then goes quiet. Archived and already-completed
        habits are excluded.
      produces:
      - application/json
      responses:
        "200":
          description: Habits due today
          schema:
            $ref: '#/definitions/habit.HabitListEnvelope'
      security:
      - BearerAuth: []
      summary: Habits due today
      tags:
      - habits
  /notes:
    get:
      description: Returns the project's notes ordered by updated_at DESC. Each item
        carries a 200-char excerpt and NO content — never render a note body from
        this response. Notes are Free and unlimited.
      parameters:
      - description: Project to list notes for
        in: query
        name: projectId
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: Notes with excerpts
          schema:
            $ref: '#/definitions/note.NoteListEnvelope'
        "404":
          description: RESOURCE_NOT_FOUND (project missing or not owned)
          schema:
            $ref: '#/definitions/note.ErrorEnvelope'
        "422":
          description: INVALID_INPUT (projectId missing)
          schema:
            $ref: '#/definitions/note.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: List a project's notes
      tags:
      - notes
    post:
      consumes:
      - application/json
      description: Files a new note under a project. content is optional (defaults
        to an empty document). content_text is always server-derived — a client-sent
        value is rejected. Notes are Free and unlimited; this endpoint never returns
        PLAN_LIMIT_EXCEEDED. Bodies over 1 MB are refused.
      parameters:
      - description: Note to create
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/note.CreateNoteRequest'
      produces:
      - application/json
      responses:
        "201":
          description: The created note
          schema:
            $ref: '#/definitions/note.NoteDetailEnvelope'
        "404":
          description: RESOURCE_NOT_FOUND (project missing or not owned)
          schema:
            $ref: '#/definitions/note.ErrorEnvelope'
        "422":
          description: INVALID_INPUT (bad title/content/projectId, or body over 1
            MB)
          schema:
            $ref: '#/definitions/note.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Create a note
      tags:
      - notes
  /notes/{id}:
    delete:
      description: Hard-deletes a note. Cross-user access returns 404, never 403.
      parameters:
      - description: Note ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "204":
          description: Deleted
        "404":
          description: RESOURCE_NOT_FOUND
          schema:
            $ref: '#/definitions/note.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Delete a note
      tags:
      - notes
    get:
      description: Retrieves a single note including its full document. Cross-user
        access returns 404, never 403.
      parameters:
      - description: Note ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: The note with its content
          schema:
            $ref: '#/definitions/note.NoteDetailEnvelope'
        "404":
          description: RESOURCE_NOT_FOUND
          schema:
            $ref: '#/definitions/note.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Get a note
      tags:
      - notes
    patch:
      consumes:
      - application/json
      description: Partial update guarded by optimistic concurrency. version is REQUIRED
        and must match the stored row; a stale version returns 409 and writes nothing.
        On 409 a client must surface a conflict state and stop autosaving — never
        retry silently. A successful save increments version.
      parameters:
      - description: Note ID
        in: path
        name: id
        required: true
        type: string
      - description: Fields to change plus the version last read
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/note.UpdateNoteRequest'
      produces:
      - application/json
      responses:
        "200":
          description: The updated note at its new version
          schema:
            $ref: '#/definitions/note.NoteDetailEnvelope'
        "404":
          description: RESOURCE_NOT_FOUND
          schema:
            $ref: '#/definitions/note.ErrorEnvelope'
        "409":
          description: CONFLICT (stale version — the note moved on)
          schema:
            $ref: '#/definitions/note.ErrorEnvelope'
        "422":
          description: INVALID_INPUT (missing version, bad title/content, or body
            over 1 MB)
          schema:
            $ref: '#/definitions/note.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Update a note
      tags:
      - notes
  /notes/{id}/backlinks:
    get:
      description: Returns the list-shape view (no content) of every note that mentions
        the given note. Empty array, not null, when there are no backlinks. Cross-user
        access returns 404, never 403.
      parameters:
      - description: Note ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: Notes that link to this one
          schema:
            $ref: '#/definitions/note.NoteListEnvelope'
        "404":
          description: RESOURCE_NOT_FOUND
          schema:
            $ref: '#/definitions/note.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: List a note's backlinks
      tags:
      - notes
  /notes/search:
    get:
      description: Returns up to 10 title matches for the @-mention autocomplete,
        row-isolated to the requesting user. excludeId omits the note currently being
        edited (a note cannot usefully mention itself). Notes are Free and unlimited;
        this endpoint never returns PLAN_LIMIT_EXCEEDED.
      parameters:
      - description: Search term
        in: query
        name: q
        required: true
        type: string
      - description: Note id to omit from results (the note being edited)
        in: query
        name: excludeId
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: Matching notes
          schema:
            $ref: '#/definitions/note.MentionSearchEnvelope'
      security:
      - BearerAuth: []
      summary: Search-as-you-type for @note mentions
      tags:
      - notes
  /notifications:
    get:
      description: Lists the user's notifications, cursor-paginated, newest first,
        with an optional read filter.
      parameters:
      - description: Filter by read state
        in: query
        name: isRead
        type: boolean
      - description: Page size (1–100)
        in: query
        name: limit
        type: integer
      - description: Pagination cursor
        in: query
        name: cursor
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: Paginated notifications
          schema:
            $ref: '#/definitions/notification.NotificationListEnvelope'
        "400":
          description: INVALID_INPUT (bad limit/cursor)
          schema:
            $ref: '#/definitions/notification.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: List notifications
      tags:
      - notifications
  /notifications/{id}:
    delete:
      description: Hard-deletes a notification. Cross-user access returns 404.
      parameters:
      - description: Notification ID
        in: path
        name: id
        required: true
        type: string
      responses:
        "204":
          description: No Content
        "404":
          description: NOTIFICATION_NOT_FOUND
          schema:
            $ref: '#/definitions/notification.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Dismiss a notification
      tags:
      - notifications
  /notifications/{id}/read:
    patch:
      description: Marks a single notification read. Cross-user access returns 404.
      parameters:
      - description: Notification ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: The updated notification
          schema:
            $ref: '#/definitions/notification.NotificationEnvelope'
        "404":
          description: NOTIFICATION_NOT_FOUND
          schema:
            $ref: '#/definitions/notification.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Mark a notification read
      tags:
      - notifications
  /notifications/preferences:
    get:
      description: Returns the user's notification preferences. An absent row yields
        the defaults ({ emailDigest:true, pushEnabled:false, smsEnabled:false, morningDigestEnabled:true,
        eveningDigestEnabled:true, streaksEnabled:true }). Push/SMS toggles are stored
        but inert until E-037.
      produces:
      - application/json
      responses:
        "200":
          description: The user's preferences
          schema:
            $ref: '#/definitions/notification.PreferencesEnvelope'
      security:
      - BearerAuth: []
      summary: Get notification preferences
      tags:
      - notifications
    put:
      consumes:
      - application/json
      description: Partially or fully updates the user's notification preferences
        (lazy upsert). morningHour must be 5–11, eveningHour 18–22. Push/SMS toggles
        persist but have no delivery path until E-037.
      parameters:
      - description: Fields to update (all optional)
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/notification.UpdatePreferences'
      produces:
      - application/json
      responses:
        "200":
          description: The updated preferences
          schema:
            $ref: '#/definitions/notification.PreferencesEnvelope'
        "400":
          description: INVALID_INPUT (bad body or hour out of range)
          schema:
            $ref: '#/definitions/notification.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Update notification preferences
      tags:
      - notifications
  /notifications/push/subscribe:
    delete:
      consumes:
      - application/json
      description: Removes the user's Web Push subscription for the given endpoint.
        Idempotent.
      parameters:
      - description: Endpoint to remove
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/notification.SubscribeRequest'
      responses:
        "204":
          description: Subscription removed
      security:
      - BearerAuth: []
      summary: Unsubscribe from web push
      tags:
      - notifications
    post:
      consumes:
      - application/json
      description: Stores a browser Web Push subscription for the user (Pro-only).
        Upserts on endpoint. Free-plan callers get PLAN_LIMIT_EXCEEDED.
      parameters:
      - description: Push subscription (endpoint + keys)
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/notification.SubscribeRequest'
      responses:
        "201":
          description: Subscription stored
        "403":
          description: PLAN_LIMIT_EXCEEDED (free plan)
          schema:
            $ref: '#/definitions/notification.ErrorEnvelope'
        "422":
          description: INVALID_INPUT (missing endpoint/keys)
          schema:
            $ref: '#/definitions/notification.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Subscribe to web push
      tags:
      - notifications
  /notifications/read-all:
    patch:
      description: Marks every unread notification read and returns how many were
        updated.
      produces:
      - application/json
      responses:
        "200":
          description: Number marked read
          schema:
            $ref: '#/definitions/notification.CountEnvelope'
      security:
      - BearerAuth: []
      summary: Mark all notifications read
      tags:
      - notifications
  /notifications/unread-count:
    get:
      description: Returns the number of unread notifications — the cheap poll target
        for the bell badge.
      produces:
      - application/json
      responses:
        "200":
          description: Unread count
          schema:
            $ref: '#/definitions/notification.UnreadCountEnvelope'
      security:
      - BearerAuth: []
      summary: Unread notification count
      tags:
      - notifications
  /projects:
    get:
      description: Lists the user's projects across all areas, cursor-paginated, with
        optional filters.
      parameters:
      - description: Search by project name
        in: query
        name: q
        type: string
      - description: Filter by area
        in: query
        name: areaId
        type: string
      - description: Filter by status (active|completed|archived)
        in: query
        name: status
        type: string
      - description: Filter by favorite
        in: query
        name: isFavorite
        type: boolean
      - description: Page size (1–100)
        in: query
        name: limit
        type: integer
      - description: Pagination cursor
        in: query
        name: cursor
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: Paginated projects
          schema:
            $ref: '#/definitions/project.ProjectListEnvelope'
        "400":
          description: INVALID_INPUT
          schema:
            $ref: '#/definitions/project.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: List projects
      tags:
      - projects
  /projects/{id}:
    delete:
      description: Deletes a project; its tasks are cascade-deleted.
      parameters:
      - description: Project ID
        in: path
        name: id
        required: true
        type: string
      responses:
        "204":
          description: No Content
        "404":
          description: PROJECT_NOT_FOUND
          schema:
            $ref: '#/definitions/project.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Delete a project
      tags:
      - projects
    get:
      description: Retrieves a single project by ID. Cross-user access returns 404.
      parameters:
      - description: Project ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: The project
          schema:
            $ref: '#/definitions/project.ProjectEnvelope'
        "404":
          description: PROJECT_NOT_FOUND
          schema:
            $ref: '#/definitions/project.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Get a project
      tags:
      - projects
    patch:
      consumes:
      - application/json
      description: Partial update of a project. Setting areaId moves the project between
        areas (null detaches).
      parameters:
      - description: Project ID
        in: path
        name: id
        required: true
        type: string
      - description: Fields to update (all optional)
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/project.UpdateProjectRequest'
      produces:
      - application/json
      responses:
        "200":
          description: The updated project
          schema:
            $ref: '#/definitions/project.ProjectEnvelope'
        "404":
          description: PROJECT_NOT_FOUND
          schema:
            $ref: '#/definitions/project.ErrorEnvelope'
        "409":
          description: DUPLICATE_NAME
          schema:
            $ref: '#/definitions/project.ErrorEnvelope'
        "422":
          description: INVALID_INPUT
          schema:
            $ref: '#/definitions/project.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Update a project
      tags:
      - projects
  /projects/{projectId}/tasks:
    get:
      description: Lists the tasks within a project, with optional filtering, sorting
        and case-insensitive ILIKE search over title+notes.
      parameters:
      - description: Project ID
        in: path
        name: projectId
        required: true
        type: string
      - description: Filter by status (active|done|cancelled)
        in: query
        name: status
        type: string
      - description: Filter by priority (low|medium|high)
        in: query
        name: priority
        type: string
      - description: Filter by energy (low|medium|deep)
        in: query
        name: energy
        type: string
      - description: ILIKE search over title + notes
        in: query
        name: search
        type: string
      - description: Sort field (displayOrder|scheduledFor|priority|title|createdAt|energy)
        in: query
        name: sortField
        type: string
      - description: Sort order (asc|desc)
        in: query
        name: sortOrder
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: List of tasks
          schema:
            $ref: '#/definitions/task.TaskListEnvelope'
        "400":
          description: INVALID_INPUT (bad sortField/sortOrder or filter)
          schema:
            $ref: '#/definitions/task.ErrorEnvelope'
        "404":
          description: PROJECT_NOT_FOUND
          schema:
            $ref: '#/definitions/task.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: List tasks in a project
      tags:
      - tasks
    post:
      consumes:
      - application/json
      description: Creates a task in a project. Title-only is valid (quick-add); other
        fields default server-side (status=active, priority=medium, energy=medium,
        rollsOver=true). Free plan allows 50 active tasks per project.
      parameters:
      - description: Project ID
        in: path
        name: projectId
        required: true
        type: string
      - description: Task to create (only title is required)
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/task.CreateTaskRequest'
      produces:
      - application/json
      responses:
        "201":
          description: The created task
          schema:
            $ref: '#/definitions/task.TaskEnvelope'
        "403":
          description: PLAN_LIMIT_EXCEEDED
          schema:
            $ref: '#/definitions/task.ErrorEnvelope'
        "404":
          description: PROJECT_NOT_FOUND
          schema:
            $ref: '#/definitions/task.ErrorEnvelope'
        "422":
          description: INVALID_INPUT / INVALID_STATUS / INVALID_PRIORITY
          schema:
            $ref: '#/definitions/task.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Create a task
      tags:
      - tasks
  /projects/reorder:
    patch:
      consumes:
      - application/json
      description: Bulk-updates project displayOrder in one transaction (atomic; a
        foreign id rolls the whole batch back).
      parameters:
      - description: New order (id → displayOrder)
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/project.ReorderRequest'
      produces:
      - application/json
      responses:
        "200":
          description: Number of rows updated
          schema:
            $ref: '#/definitions/project.ReorderResultEnvelope'
        "404":
          description: PROJECT_NOT_FOUND (a foreign id — whole batch rolled back)
          schema:
            $ref: '#/definitions/project.ErrorEnvelope'
        "422":
          description: INVALID_INPUT
          schema:
            $ref: '#/definitions/project.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Reorder projects
      tags:
      - projects
  /search:
    get:
      description: Searches the user's tasks, projects and areas via PostgreSQL full-text
        (plainto_tsquery), ranked by relevance.
      parameters:
      - description: Search term (2–100 chars)
        in: query
        name: q
        required: true
        type: string
      - description: 'Comma-separated groups: task,project,area,note (omit for all)'
        in: query
        name: types
        type: string
      - description: Per-type cap (default 10, max 50)
        in: query
        name: limit
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: Grouped results
          schema:
            $ref: '#/definitions/search.SearchEnvelope'
        "400":
          description: INVALID_INPUT
          schema:
            $ref: '#/definitions/search.ErrorEnvelope'
        "401":
          description: UNAUTHORIZED
          schema:
            $ref: '#/definitions/search.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Full-text search
      tags:
      - search
  /tasks:
    get:
      description: Flat list of the user's tasks scheduled within an inclusive date
        range, ordered by (scheduledFor, scheduledTime NULLS FIRST, displayOrder).
        Unlike Time Spread no roll-forward is applied — a task is returned on the
        date it is actually scheduled for. Both bounds are required and the span is
        capped at 62 days.
      parameters:
      - description: Inclusive start date (YYYY-MM-DD)
        in: query
        name: scheduledFrom
        required: true
        type: string
      - description: Inclusive end date (YYYY-MM-DD)
        in: query
        name: scheduledTo
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: Tasks in the range
          schema:
            $ref: '#/definitions/task.TaskListEnvelope'
        "422":
          description: INVALID_INPUT (missing bound, bad date, or span > 62 days)
          schema:
            $ref: '#/definitions/task.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: List tasks in a date range (calendar)
      tags:
      - tasks
  /tasks/{id}:
    delete:
      description: Hard-deletes a task; its subtasks cascade.
      parameters:
      - description: Task ID
        in: path
        name: id
        required: true
        type: string
      responses:
        "204":
          description: No Content
        "404":
          description: TASK_NOT_FOUND
          schema:
            $ref: '#/definitions/task.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Delete a task
      tags:
      - tasks
    get:
      description: Retrieves a single task by ID. Cross-user access returns 404 (no
        existence leak).
      parameters:
      - description: Task ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: The task
          schema:
            $ref: '#/definitions/task.TaskEnvelope'
        "404":
          description: TASK_NOT_FOUND
          schema:
            $ref: '#/definitions/task.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Get a task
      tags:
      - tasks
    patch:
      consumes:
      - application/json
      description: Partial update of any mutable field. status→done sets completedAt
        server-side; moving away from done clears it. A PATCH that moves a task into
        active is subject to the plan limit.
      parameters:
      - description: Task ID
        in: path
        name: id
        required: true
        type: string
      - description: Fields to update (all optional)
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/task.UpdateTaskRequest'
      produces:
      - application/json
      responses:
        "200":
          description: The updated task
          schema:
            $ref: '#/definitions/task.TaskEnvelope'
        "403":
          description: PLAN_LIMIT_EXCEEDED
          schema:
            $ref: '#/definitions/task.ErrorEnvelope'
        "404":
          description: TASK_NOT_FOUND
          schema:
            $ref: '#/definitions/task.ErrorEnvelope'
        "422":
          description: INVALID_INPUT / INVALID_STATUS / INVALID_PRIORITY
          schema:
            $ref: '#/definitions/task.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Update a task
      tags:
      - tasks
  /tasks/{id}/mark-missed:
    patch:
      description: Reaps one recurring occurrence to (cancelled, missed) — the same
        terminal state the overdue sweep sets automatically once its due date passes,
        just triggered now instead of at the next local midnight. Only eligible for
        a still-active recurring occurrence due today or earlier.
      parameters:
      - description: Task ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: The reaped task
          schema:
            $ref: '#/definitions/task.TaskEnvelope'
        "404":
          description: TASK_NOT_FOUND
          schema:
            $ref: '#/definitions/task.ErrorEnvelope'
        "422":
          description: TASK_NOT_MISSABLE — not recurring, not active, already reaped,
            or its occurrence date is still in the future
          schema:
            $ref: '#/definitions/task.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Manually mark a recurring occurrence missed
      tags:
      - tasks
  /tasks/{id}/reorder:
    patch:
      consumes:
      - application/json
      description: Moves a task to a target displayOrder; siblings in the project
        are repacked to a contiguous 0..n-1 sequence (transactional).
      parameters:
      - description: Task ID
        in: path
        name: id
        required: true
        type: string
      - description: Target displayOrder
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/task.ReorderOneRequest'
      produces:
      - application/json
      responses:
        "200":
          description: The moved task
          schema:
            $ref: '#/definitions/task.TaskEnvelope'
        "404":
          description: TASK_NOT_FOUND
          schema:
            $ref: '#/definitions/task.ErrorEnvelope'
        "422":
          description: INVALID_INPUT
          schema:
            $ref: '#/definitions/task.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Reorder a task
      tags:
      - tasks
  /tasks/{id}/schedule:
    patch:
      consumes:
      - application/json
      description: Sets the soft scheduledFor intention, optional scheduledTime (HH:MM,
        Pro-only) and rollsOver flag. scheduledFor null (or absent) unschedules. A
        bad date returns 400 INVALID_DATE.
      parameters:
      - description: Task ID
        in: path
        name: id
        required: true
        type: string
      - description: scheduledFor (ISO date or null) + optional scheduledTime + rollsOver
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/task.ScheduleRequest'
      produces:
      - application/json
      responses:
        "200":
          description: The updated task
          schema:
            $ref: '#/definitions/task.TaskEnvelope'
        "400":
          description: INVALID_DATE
          schema:
            $ref: '#/definitions/task.ErrorEnvelope'
        "403":
          description: PLAN_LIMIT_EXCEEDED (free plan setting a time)
          schema:
            $ref: '#/definitions/task.ErrorEnvelope'
        "404":
          description: TASK_NOT_FOUND
          schema:
            $ref: '#/definitions/task.ErrorEnvelope'
        "422":
          description: INVALID_INPUT (malformed or non-snapped time)
          schema:
            $ref: '#/definitions/task.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Schedule a task (soft)
      tags:
      - tasks
  /tasks/{id}/skip:
    post:
      description: Marks the live recurring occurrence skipped — the user deliberately
        opting out, without breaking their streak. Unlike mark-missed, eligible any
        time (including ahead of the occurrence's due date). Best-effort cancels any
        pending notification tied to the task.
      parameters:
      - description: Task ID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: The skipped task
          schema:
            $ref: '#/definitions/task.TaskEnvelope'
        "404":
          description: TASK_NOT_FOUND
          schema:
            $ref: '#/definitions/task.ErrorEnvelope'
        "409":
          description: TASK_NOT_SKIPPABLE — not recurring, not the live instance,
            or not active
          schema:
            $ref: '#/definitions/task.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Skip a recurring occurrence
      tags:
      - tasks
  /tasks/{id}/status:
    patch:
      consumes:
      - application/json
      description: Status-only update (checkbox toggle, cancel). Same completedAt
        side-effects and plan-limit semantics as the full PATCH.
      parameters:
      - description: Task ID
        in: path
        name: id
        required: true
        type: string
      - description: New status
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/task.SetStatusRequest'
      produces:
      - application/json
      responses:
        "200":
          description: The updated task
          schema:
            $ref: '#/definitions/task.TaskEnvelope'
        "403":
          description: PLAN_LIMIT_EXCEEDED
          schema:
            $ref: '#/definitions/task.ErrorEnvelope'
        "404":
          description: TASK_NOT_FOUND
          schema:
            $ref: '#/definitions/task.ErrorEnvelope'
        "422":
          description: INVALID_INPUT / INVALID_STATUS
          schema:
            $ref: '#/definitions/task.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Set task status (shorthand)
      tags:
      - tasks
  /tasks/{taskId}/subtasks:
    get:
      description: Lists a task's subtasks, ordered by position. Parent-task ownership
        is enforced.
      parameters:
      - description: Parent task ID
        in: path
        name: taskId
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: List of subtasks
          schema:
            $ref: '#/definitions/task.SubtaskListEnvelope'
        "404":
          description: RESOURCE_NOT_FOUND (task not found / not owned)
          schema:
            $ref: '#/definitions/task.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: List subtasks
      tags:
      - subtasks
    post:
      consumes:
      - application/json
      description: Creates a subtask under a task. Appends to the end unless position
        is given.
      parameters:
      - description: Parent task ID
        in: path
        name: taskId
        required: true
        type: string
      - description: Subtask (title required, position optional)
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/task.CreateSubtaskRequest'
      produces:
      - application/json
      responses:
        "201":
          description: The created subtask
          schema:
            $ref: '#/definitions/task.SubtaskEnvelope'
        "404":
          description: RESOURCE_NOT_FOUND (task not found / not owned)
          schema:
            $ref: '#/definitions/task.ErrorEnvelope'
        "422":
          description: INVALID_INPUT
          schema:
            $ref: '#/definitions/task.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Create a subtask
      tags:
      - subtasks
  /tasks/{taskId}/subtasks/{subtaskId}:
    delete:
      description: Deletes a subtask. Parent-task ownership enforced.
      parameters:
      - description: Parent task ID
        in: path
        name: taskId
        required: true
        type: string
      - description: Subtask ID
        in: path
        name: subtaskId
        required: true
        type: string
      responses:
        "204":
          description: No Content
        "404":
          description: RESOURCE_NOT_FOUND
          schema:
            $ref: '#/definitions/task.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Delete a subtask
      tags:
      - subtasks
    patch:
      consumes:
      - application/json
      description: Partial update of a subtask (title, done, position). Parent-task
        ownership enforced.
      parameters:
      - description: Parent task ID
        in: path
        name: taskId
        required: true
        type: string
      - description: Subtask ID
        in: path
        name: subtaskId
        required: true
        type: string
      - description: Fields to update (all optional)
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/task.UpdateSubtaskRequest'
      produces:
      - application/json
      responses:
        "200":
          description: The updated subtask
          schema:
            $ref: '#/definitions/task.SubtaskEnvelope'
        "404":
          description: RESOURCE_NOT_FOUND
          schema:
            $ref: '#/definitions/task.ErrorEnvelope'
        "422":
          description: INVALID_INPUT
          schema:
            $ref: '#/definitions/task.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Update a subtask
      tags:
      - subtasks
  /time-spread:
    get:
      description: Buckets the user's active tasks into today/tomorrow/thisWeek with
        the no-guilt roll-forward (a past soft-scheduled task with rollsOver=true
        carries to today instead of going overdue). done/cancelled are excluded.
      parameters:
      - description: IANA timezone for day bucketing (e.g. Asia/Jerusalem); defaults
          to UTC
        in: query
        name: tz
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: Three-bucket task lists
          schema:
            $ref: '#/definitions/task.TimeSpreadEnvelope'
        "400":
          description: INVALID_INPUT — unknown timezone
          schema:
            $ref: '#/definitions/task.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Time spread (today / tomorrow / this week)
      tags:
      - tasks
  /users/me:
    delete:
      description: Soft-deletes the authenticated user (sets deleted_at) and revokes
        all refresh tokens.
      produces:
      - application/json
      responses:
        "204":
          description: Account deleted; all sessions revoked
        "401":
          description: UNAUTHORIZED
          schema:
            $ref: '#/definitions/auth.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Delete the current account
      tags:
      - users
    patch:
      consumes:
      - application/json
      description: Updates profile fields. All fields are optional; omitted fields
        are left unchanged.
      parameters:
      - description: Fields to update
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/auth.UpdateMeRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/auth.UserEnvelope'
        "401":
          description: UNAUTHORIZED
          schema:
            $ref: '#/definitions/auth.ErrorEnvelope'
        "422":
          description: INVALID_INPUT
          schema:
            $ref: '#/definitions/auth.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Update the current user
      tags:
      - users
  /users/profile:
    get:
      description: Returns the authenticated user's profile.
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/auth.UserEnvelope'
        "401":
          description: UNAUTHORIZED
          schema:
            $ref: '#/definitions/auth.ErrorEnvelope'
      security:
      - BearerAuth: []
      summary: Get the current user
      tags:
      - users
schemes:
- http
- https
securityDefinitions:
  BearerAuth:
    description: JWT access token as "Bearer <token>".
    in: header
    name: Authorization
    type: apiKey
swagger: "2.0"
